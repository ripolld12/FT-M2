import { useLocation, useNavigate, useParams } from "react-router-dom"


const About = () => {
    // const location = useLocation()
    // console.log(location); // Este hook que tiene el router, es un objeto que tiene unas propiedades, como el key, el pathname, etc...

    // const navigate = useNavigate()
    // // navigate('/profile') yo puedo usar esto es un boton para que con un onClick y le ponemos el naigate
    // console.log(navigate); // Este hook que tiene el router, es una funcion que recibe dos parametros, uno es el to (path) y el otro es options (que puede estar o no)
    
    

    return (
        <>
        <h1>Este es el componente About</h1></>
    )
}

export default About